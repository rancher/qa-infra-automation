---
collections:
  - name: kubernetes.core
    version: "5.0.0"
  - name: community.general
    version: "10.1.0"
  - name: cloud.terraform
    version: "2.2.0"
  - name: ansible.posix
    version: "1.6.2"
  - name: amazon.aws
    version: "8.2.0"
  # win_* modules for RKE2 Windows agents. Reached over SSH with
  # ansible_shell_type=powershell, so pywinrm is not required.
  - name: ansible.windows
    version: "3.7.0"
