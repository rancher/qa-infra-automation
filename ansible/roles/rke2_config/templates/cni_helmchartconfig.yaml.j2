# HelmChartConfig for the bundled RKE2 CNI chart
# Generated by Ansible - rke2_config role
# Overrides values for the rke2-{{ rke2_cni }} chart RKE2 deploys on first start.
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: rke2-{{ rke2_cni }}
  namespace: kube-system
spec:
  valuesContent: |-
{{ rke2_cni_values_content | trim | indent(4, true) }}
